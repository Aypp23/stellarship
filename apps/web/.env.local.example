NEXT_PUBLIC_SOROBAN_RPC_URL="https://soroban-testnet.stellar.org"
NEXT_PUBLIC_NETWORK_PASSPHRASE="Test SDF Network ; September 2015"

# Deploy /Users/aomine/Desktop/stellar/contracts/target/wasm32v1-none/release/zk_battleship.wasm to Testnet,
# then set the deployed contract id here:
NEXT_PUBLIC_ZK_BATTLESHIP_CONTRACT_ID=""

# Optional: only needed if you want "open invites" (no opponent address yet).
# Must be a funded, existing Testnet account address (G...).
NEXT_PUBLIC_SIMULATION_SOURCE_ADDRESS=""

# Optional: socket.io relay for realtime move exchange during matches.
NEXT_PUBLIC_RELAY_URL="http://localhost:3001"
